تونل WSSMux: یک راهنمای کامل
یک ابزار ساده، مدرن و قدرتمند برای ایجاد تونل‌های TCP چندگانه از طریق یک اتصال WebSocket امن (WSS). این روش برای عبور از فایروال‌ها، کاهش تعداد پورت‌های باز و پنهان‌سازی ترافیک ایده‌آل است.

برخلاف ابزارهای قدیمی مانند xwsmux، این پروژه به صورت فعال توسعه داده می‌شود و نصب آن به دلیل ارائه فایل اجرایی آماده، بسیار ساده است.

✨ ویژگی‌های کلیدی
نصب آسان: بدون نیاز به کامپایل! فقط یک فایل اجرایی واحد که روی هر سیستم لینوکسی کار می‌کند.

امنیت داخلی: استفاده از پروتکل WSS برای رمزنگاری تمام ترافیک بین کلاینت و سرور با TLS.

کارایی بالا: نوشته شده با زبان Go برای مدیریت بهینه و همزمان اتصالات شبکه با کمترین سربار (Overhead).

انعطاف‌پذیری: قابلیت تعریف چندین تونل پورت به صورت همزمان فقط با یک دستور.

پایداری: طراحی شده برای اتصالات پایدار و طولانی مدت با قابلیت اتصال مجدد خودکار.


wget https://raw.githubusercontent.com/AliTabarY/WSSMUX/main/wss.sh -O wss.sh && chmod +x wss.sh && ./wss.sh
